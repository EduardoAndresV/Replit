//condición ? expresión_si_true : expresión_si_false;

let mensaje = (edad >= 18) ? "Mayor de edad" : "Menor de edad";


const nota = 85;
const resultado = (nota >= 90) ? "Excelente" 
                : (nota >= 70) ? "Aprobado" 
                : "Reprobado";
console.log(resultado);